# Artifacts

This repository intentionally does not keep large binary release artifacts in git.

Recommended release assets:
- `bettercap`
- `libusb1.0.so`
- `libusb-1.0.so`
- packaged tar.gz if desired

Suggested source of truth for publishing:
- GitHub Releases attached to this repository

Original local reference paths during reconstruction:
- `/root/build-out/bettercap-android-arm64-v2415-package/`
- `/root/research/android-libusb/libusb/android/libs/arm64-v8a/libusb1.0.so`
