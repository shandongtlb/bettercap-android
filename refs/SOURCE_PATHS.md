# Source paths used during reconstruction

Local source references on the build machine:

- bettercap source:
  - `/root/research/bettercap`
- android libusb source:
  - `/root/research/android-libusb/libusb`
- packaged output reference:
  - `/root/build-out/bettercap-android-arm64-v2415-package`

This repository keeps source/tooling context and packaging notes, but release binaries should be uploaded through GitHub Releases.
